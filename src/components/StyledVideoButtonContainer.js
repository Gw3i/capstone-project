import styled from 'styled-components';

const StyledVideoButtonContainer = styled.div`
	display: flex;
	position: absolute;
	top: 80px;
	right: 20px;
	flex-direction: column;
	gap: 10px;
`;

export default StyledVideoButtonContainer;
